<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Usuario">
    <select id="getTodas" resultType="modelo.beans.Usuario">
        SELECT * FROM empenio.usuario;
    </select>
    
    <select id="buscarCategoria" resultType="modelo.beans.Usuario" parameterType="string">
        SELECT * FROM empenio.categoria
        WHERE nombre LIKE CONCAT(#{nombre},'%');
    </select>
    
    <insert id="registrar" parameterType="map">
        INSERT INTO empenio.usuario(nombreCompleto, contrasenia, rol, fechaIngreso)
        VALUES(#{nombreCompleto},#{contrasenia},#{rol},#{fechaIngreso});
    </insert>
</mapper>