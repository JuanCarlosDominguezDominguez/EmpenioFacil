<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Categoria">
    
    <insert id="registrar" parameterType="map">
        INSERT INTO empenio.categorias (nombre, Categorias_idCategoria)
        VALUES(#{nombre}, #{idCategoria});
    </insert>
    
    <select id="buscarCategoriasRoles" resultType="modelo.beans.Categoria">
        SELECT * FROM empenio.categorias WHERE Categorias_idCategoria = '1' ORDER BY nombre;
    </select>
    
    <select id="obtenerRol" resultType="modelo.beans.Categoria" parameterType="string">
        SELECT * FROM empenio.categorias WHERE idCategoria = #{idCategoria};
    </select>
    
    <select id="buscarCategoriasPrendas" resultType="modelo.beans.Categoria">
        SELECT * FROM empenio.categorias 
        WHERE idCategoria = '2' OR idCategoria = '3'  ORDER BY nombre;
    </select>
    
    <select id="buscarCategoriasPrendasSecundarias" resultType="modelo.beans.Categoria">
        SELECT * FROM empenio.categorias 
        WHERE Categorias_idCategoria = '2' OR Categorias_idCategoria = '3'  ORDER BY nombre;
    </select>
    
    <select id="buscarCategoriasVentaApartado" resultType="modelo.beans.Categoria">
        SELECT * FROM empenio.categorias 
        WHERE Categorias_idCategoria = '4' ORDER BY nombre;
    </select>
    
    <select id="buscarCategoriasPeriodos" resultType="modelo.beans.Categoria">
        SELECT * FROM empenio.categorias
        WHERE Categorias_idCategoria = '5' ORDER BY nombre;
    </select>
    
    <select id="buscarCategoriasOcupacion" resultType="modelo.beans.Categoria">
        SELECT * FROM empenio.categorias
        WHERE Categorias_idCategoria = '6' ORDER BY nombre;
    </select>
    
    <select id="busquedaGenerica" resultType="modelo.beans.Categoria" parameterType="string">
        SELECT * FROM empenio.categorias
        WHERE #{criterios};
    </select>
    
    <select id="buscarTodasLasCategoriasDePrendas" resultType="modelo.beans.Categoria" >
        SELECT * FROM empenio.categorias WHERE idCategoria = 2 OR idCategoria = 3
        OR Categorias_idCategoria = 2 OR Categorias_idCategoria = 3;
    </select>
    
    <delete id="eliminar" parameterType="int">
        DELETE FROM empenio.categorias WHERE idCategoria = #{idCategoria};
    </delete>
    
    <update id="actualizar" parameterType="map">
        UPDATE empenio.categorias SET nombre = #{nombre}
        WHERE idCategoria = #{idCategoria};    
    </update>
    
</mapper>
